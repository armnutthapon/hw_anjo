import 'package:flutter/material.dart';

class Variable extends StatelessWidget {
  Variable({Key? key}) : super(key: key);
  var name = TextEditingController();
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
